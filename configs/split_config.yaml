
paths:
  root_dir: "data/short"
  output_dir: "data/short_out"
  temp_dir: "temp"

vad:
  aggressiveness: 2  # 0-3
  min_silence_ms: 500
  min_segment_ms: 1000
  sample_rate: 16000
  resample: True
  frame_duration: 30
  file_format: "wav"

processing:
  min_len: 0.0
  segment_name: "segment"
  max_workers: 8
  segment_subfolders: False
  batch_size: 10