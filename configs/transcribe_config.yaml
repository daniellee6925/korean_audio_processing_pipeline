model:
  name: "base"  # GPU 7 has plenty of space!
  device: "cuda"

data:
  root_dir: "/workspace/wavs_20250416_013301_segments"
  recursive: true
  extension: "wav"
  output_prefix: ""

processing:
  num_gpus: 1
  num_actors: 2           # 2 actors (0.25 GPU each)
  batch_size: 8           # 8 files per batch
  dynamic_batching: false
  use_fp16: true
  ray_temp_dir: "/workspace/ray_tmp"

transcription:
  language: "ko"
  task: "transcribe"
  temperature: 0.0                     # sampling temperature for decoding
  beam_size: 5                         # beam search size
  patience: 1.0                        # beam search patience